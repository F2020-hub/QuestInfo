import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    heading: `Open Sans`,
    body: `sans-serif`,
  },
  background:{
    background:"#f3f2f1"
  }
})

export default theme